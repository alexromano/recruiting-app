A simple web app to track new roles at companies of interest.

The app uses Nest.js on the backend and Next.js on the frontend.

## Running locally
### Backend
```bash
cd backend
npm i
npm run start
```
### Frontend
```bash
cd frontend
npm i
npm run start
```